# Lock npm version (compatible with Node 20)
# Use npm 10.x (bundled with Node 20 LTS)
engine-strict=true

# Faster installs
prefer-offline=true
audit=false

# China mirror (optional, can be removed if not needed)
# registry=https://registry.npmmirror.com
